<h4>Edit comment:</h4>

<%= form_for([@post, @comment]) do |f| %>
  
    <%= f.label :comment %><br />
    <%= f.text_area :comment %>
 
  
    <%= f.submit %>
  
<% end %>

<%= link_to 'Back', posts_path %>